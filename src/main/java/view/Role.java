package view;

/**
 * Ruolo scelto dall'utente nella schermata di selezione ruolo (Benvenuto),
 * usato per parametrizzare la schermata di login condivisa (Bentornato).
 */
public enum Role {
    MUSICIAN,
    PROMOTER
}
